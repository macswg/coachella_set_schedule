# TODO

## Open Questions

- **Changeover rows in sheet** — do we need explicit changeover rows in the Google Sheet, or can the app infer changeover from the gap between artists?

- **Load-in / On Deck overlap** — confirm whether load-in and on-deck schedule items can overlap in time, and determine how the UI and trigger logic should handle that case.

- **Google Sheet naming syntax** — confirm with producer the exact naming conventions for artist set rows and stage time rows in the sheet. This affects `is_ondeck`, `is_changeover`, and `RECORDING_ACT_PREFIX` filtering.

## Features

- **Refine `/stage` view** — update the look and layout of the stage display page.

