# Data Backend
# Which persistent store to use. One of:
#   sheets  — Google Sheets (requires GOOGLE_* settings below)
#   sqlite  — Internal SQLite DB (file at SQLITE_PATH; schedule managed in-app via /admin)
#   memory  — In-process mock data (dev only; resets on restart)
DATA_BACKEND=sheets

# Path to the SQLite DB file (only used when DATA_BACKEND=sqlite).
# The parent directory is created automatically; mount it as a volume in Docker.
SQLITE_PATH=./data/schedule.db

# Google Sheets Configuration (only used when DATA_BACKEND=sheets)
# NOTE: USE_GOOGLE_SHEETS is deprecated — use DATA_BACKEND=sheets instead.
USE_GOOGLE_SHEETS=false

# Get the spreadsheet ID from the URL: https://docs.google.com/spreadsheets/d/{SPREADSHEET_ID}/edit
GOOGLE_SHEETS_ID=your-spreadsheet-id-here

# Which tab/worksheet to use (leave empty for first sheet)
GOOGLE_SHEET_TAB=

# Path to your Google Cloud service account JSON key file
GOOGLE_SERVICE_ACCOUNT_FILE=/path/to/service-account.json

# App Configuration
STAGE_NAME=Main Stage
TIMEZONE=America/Los_Angeles

# Server Configuration
HOST=0.0.0.0
PORT=3030

# Multi-Show Configuration
# Comma-separated list of Google Sheet tabs in show order. Leave empty for single-show mode.
# Example: SHOW_TABS=W1Shw1,W1Shw2,W1Shw3
SHOW_TABS=

# Ki Pro Recorder Configuration
# IP address of the AJA Ki Pro Rack
KIPRO_IP=

# ntfy.sh Push Notifications
# Full topic URL, e.g. https://ntfy.sh/your-topic or http://your-server/your-topic
NTFY_URL=

# WeatherLink Weather Data (optional)
# Full getData URL from WeatherLink embeddable page, e.g.:
# https://www.weatherlink.com/embeddablePage/getData/{embeddable-page-id}
# Leave empty to hide the weather display entirely
WEATHER_URL=

# Recording Trigger Configuration
# Set to true to enable automatic recording triggers
RECORDING_ENABLED=false
# Minutes before scheduled start to trigger recording
RECORDING_PRE_START_MINUTES=5
# Only trigger recording for acts whose names start with this prefix (empty = all acts)
# Example: RECORDING_ACT_PREFIX=Set:
RECORDING_ACT_PREFIX=

# Art-Net Configuration
# Enable Art-Net DMX receiver for brightness control
ARTNET_ENABLED=false
ARTNET_PORT=6454
ARTNET_UNIVERSE=0
# Bit depth: 8 for single-channel (0-255), 16 for high/low byte pair (0-65535)
ARTNET_BIT_DEPTH=8
# 8-bit mode: single DMX channel
ARTNET_CHANNEL=1
# 16-bit mode: two DMX channels (high byte, low byte)
ARTNET_CHANNEL_HIGH=1
ARTNET_CHANNEL_LOW=2
# Maximum nits output of the LED wall at full brightness (varies per display)
ARTNET_MAX_NITS=11000

# Bitfocus Companion HTTP API
# Full base URL including port, e.g. http://192.168.1.100:19267
COMPANION_URL=
